import 'package:flutter/material.dart';

class InfoSpacing extends StatelessWidget {
  const InfoSpacing({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const SizedBox(height: 16.0);
}
